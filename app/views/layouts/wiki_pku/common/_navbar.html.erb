<div id="nav" class="w-950 bor_b-ccc">
	<ul>
		<li><%=link_to "首页", wiki_pku.root_path%></li>
		<%WikiPku::Tab.all.each do |tab|%>
			<li>
				<%=link_to tab.name, wiki_pku.tab_path(tab)%>
			</li>
		<%end%>
	</ul>
		<label><%=link_to "创建词条", wiki_pku.new_entry_path%></label>
	</div>
</div>
